node_modules/
.astro/
.env
.env.*
!.env.example
dist/
.dist.staging-*/
.dist.backup-*/
artifacts/
coverage/
target/
*.db
*.sqlite
*.sqlite3
logs/
tmp/
secrets/
media/

# Protocol projections are generated from contracts/ and never hand-edited.
# Keep the checked-in projections under protocol/schema/json and
# packages/api-client/src/generated for offline, version-pinned consumers.
protocol/.tmp/
protocol/.cache/
packages/api-client/.tmp/

# Legacy runtime state and development-only evidence remain machine-local.
.agent/
.local/
.agent.local/
.agent-runtime/
.agent-cache/
.agent-artifacts/
.agent-work/
.agent-*.log
.agent-*.jsonl
# Ad-hoc agent files at repository root are local-only; the versioned
# governance directory remains intentionally tracked.
.agent-*.json
.agent-*.tmp

# Generated repository adapters are versioned; all other Codex session state is local.
.codex/*
!.codex/config.toml
!.codex/agents/
.codex/agents/*
!.codex/agents/*.toml
.nx/
.claude/
.vscode/
.idea/
*.code-workspace
