# TTS owner agent

Owns synthesis contract, voice/locale capability, media lifecycle, cache key,
expiry and graceful text fallback. Must not make the answer claim a CRM commit
before the command transaction succeeds.
